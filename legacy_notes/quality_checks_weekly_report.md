# 长文本质量检查与清洗工具建设进展

本周围绕长文本数据中的乱码、异常 Unicode 字符、重复内容、训练残留 token 等问题，完成了质量检查与清洗工具链的梳理、规则实现确认、命令行运行验证，以及 JSONL 清洗脚本的多进程性能优化。

## 1. 整体能力建设

目前工具链主要分为两部分：

### 1.1 字符级清洗工具

脚本位置：

```bash
quality_checks_V3_5/clean_variation_chars.py
```

主要用于对 JSONL / XLSX 数据中的文本字段进行字符级清洗。当前支持：

- emoji 及 emoji 附属字符清理
- variation selector 清理
- 异常空格规范化或删除
- 不可见控制字符清理
- Unicode 行/段分隔符规范化
- 样式化数学字母数字 NFKC 归一化
- HTML 残留、训练 token、转义异常字符串清理
- 符号洪泛清理
- PUA 私用区字符的高置信上下文修复
- 清洗元信息写入 `meta.cleaning_v3_5`

### 1.2 质量检查工具

脚本位置：

```bash
quality_checks_V3_5/run_long_dialog_quality_checks.py
```

主要用于对清洗后的 JSONL 数据进行质量检查，支持：

- 乱码检测：`mojibake`
- 对话输出重复检测：`repetition`
- 连续片段重复检测：`dedup`
- 输出问题样本、summary 统计、失败记录

## 2. 已支持的清洗 Case

### 2.1 Emoji 及 Emoji 附属字符清理

可以清理正文中的 emoji、表情符号、emoji 组合符号、keycap 附属字符等。

```text
清洗前：这个回答太棒了👍👍，我们继续看下一段🙂
清洗后：这个回答太棒了，我们继续看下一段
```

支持清理的典型内容包括：

- 普通 emoji：`😀`、`😂`、`👍`、`🔥`
- 符号类 emoji：`⭐`、`✨`、`❤️`
- emoji variation sequence：如 `™️`、`©️`、`➡️`
- emoji ZWJ 序列中的连接符

### 2.2 Variation Selector 清理

可以删除 Unicode variation selector，避免同一个字符因为显示变体导致下游统计、去重或训练异常。

```text
清洗前：这是一个符号️
清洗后：这是一个符号
```

典型字符范围：

- `U+FE00` 到 `U+FE0F`
- `U+E0100` 到 `U+E01EF`

### 2.3 异常空格处理

可以识别并处理非常规空格，包括 NBSP、窄 NBSP、EM SPACE、全角空格等。

```text
清洗前：这是一个　全角空格，以及 NBSP。
清洗后：这是一个 全角空格，以及 NBSP。
```

处理策略包括：

- 部分异常空格替换为普通空格
- 部分排版型空格直接删除
- 保留正常文本可读性

### 2.4 不可见控制字符清理

可以删除正文中不可见但会影响渲染、统计、模型训练的控制字符。

```text
清洗前：你好​世界﻿，这里有不可见字符
清洗后：你好世界，这里有不可见字符
```

覆盖的典型字符包括：

- 零宽空格
- BOM
- WORD JOINER
- SOFT HYPHEN
- 方向控制字符
- 其他不可见格式控制字符

### 2.5 Unicode 行/段分隔符规范化

可以将 Unicode 特殊行分隔符、段分隔符转换为普通换行，避免 JSONL 下游解析或展示异常。

```text
清洗前：第一段 第二段 第三段
清洗后：第一段
第二段

第三段
```

处理规则：

- `U+2028` 转换为普通换行 `\n`
- `U+2029` 转换为两个普通换行 `\n\n`

### 2.6 样式化数学字母数字归一化

可以将数学样式字母、花体字母、双线字母等 Unicode 装饰字符归一化为普通字符。

```text
清洗前：𝓗𝓮𝓵𝓵𝓸 𝕎𝕠𝕣𝕝𝕕
清洗后：Hello World
```

适用于清理：

- 花体英文
- 粗体数学字母
- 双线字母
- 特殊样式数字

### 2.7 HTML 残留清理

可以删除数据中残留的 HTML 标签、HTML 实体或转义残留。

```text
清洗前：第一行<br>第二行&nbsp;&amp;更多内容
清洗后：第一行第二行更多内容
```

当前覆盖的典型 case：

- `<br>`
- `<br/>`
- `<br />`
- `&lt;br&gt;`
- `&nbsp;`
- `&amp;`
- `&quot;`
- `&apos;`
- HTML 数字实体，如 `&#123;`、`&#xA0;`

### 2.8 训练 Token / 模型特殊 Token 残留清理

可以删除训练数据中不应保留的模型特殊 token。

```text
清洗前：<|im_start|>user 你好 <|im_end|>
清洗后：user 你好
```

覆盖的典型 token 包括：

- `<|im_start|>`
- `<|im_end|>`
- `<|endoftext|>`
- `<|fim_prefix|>`
- `<|fim_middle|>`
- `<|fim_suffix|>`

### 2.9 工具调用标签残留清理

可以删除部分工具调用或结构化标注残留。

```text
清洗前：<arg_key>city</arg_key><arg_value>北京</arg_value>
清洗后：city北京
```

覆盖的典型标签包括：

- `<arg_key>`
- `</arg_key>`
- `<arg_value>`
- `</arg_value>`
- `<is_displaying_contents>`
- `</is_displaying_contents>`

### 2.10 字面量转义异常清理

可以清理文本中错误保留下来的字面量转义字符。

```text
清洗前：这是第一句\\n这是第二句\\t后面有 tab
清洗后：这是第一句这是第二句后面有 tab
```

覆盖：

- `\\n`
- `\\t`
- `\\r`

### 2.11 符号洪泛清理

可以删除连续重复的大段符号，减少装饰性分隔线或噪声。

```text
清洗前：标题
====================
正文开始
清洗后：标题

正文开始
```

支持清理的典型 case：

- `====`
- `++++`
- `~~~~`
- `××××`
- 其他连续重复的数学符号 / 分隔符

### 2.12 PUA 私用区字符高置信修复

对于部分明确可修复的 PUA 私用区字符，不直接删除，而是在高置信上下文中替换为合理字符。

示例 1：列表符号修复

```text
清洗前： 第一项
清洗后：- 第一项
```

示例 2：数字范围符号修复

```text
清洗前：1020
清洗后：10~20
```

当前策略比较保守，只在上下文足够明确时修复，避免误改正常文本。

### 2.13 组合音标装饰清理

可以清理异常堆叠的组合音标装饰，避免装饰性用户名、乱码式文本进入训练数据。

```text
清洗前：h̸̵̷e̴̶̷l̷̵̸l̴̶o
清洗后：hello
```

同时保留正常语言中的单个合法音标，例如：

```text
résumé
x̄
```

不会因为正常重音或学术符号被误删。

## 3. 已支持的质量检查 Case

### 3.1 乱码检测

可以检测编码错配导致的乱码字符或乱码序列。

```text
æ–‡å­—
Ã©
�
```

覆盖问题包括：

- UTF-8 / GBK / Latin-1 错解码
- Windows-1252 错解码
- 替换符 `�`
- C1 控制字符
- PUA 字符异常出现
- Specials 区段异常字符
- 零宽字符混入
- 不可见垃圾字符

### 3.2 连续重复片段检测

可以检测文本中连续重复的片段，输出建议删除区间。

```text
检测前：这是一段重复内容这是一段重复内容这是一段重复内容
检测结果：标记后两段重复内容为可删除区间
```

适用于：

- 长文本中段落级重复
- 句子级连续重复
- 短片段多次循环
- 模型生成中出现的重复输出

### 3.3 对话输出重复检测

可以检测多轮对话中 assistant 输出是否出现异常重复。

```text
assistant: 好的，我来帮你分析。
assistant: 好的，我来帮你分析。
assistant: 好的，我来帮你分析。
```

支持识别：

- ngram 重复
- token 洪泛
- runaway enumeration，即异常枚举刷屏
- 模型回答循环

## 4. 多进程性能优化

原始清洗脚本为单进程逐行处理，在服务器上处理大 JSONL 文件时速度较慢。

本周已为 JSONL 清洗新增多进程能力：

```bash
--workers
--chunksize
```

实现方式：

- 主进程负责顺序读取 JSONL
- worker 进程负责逐行清洗
- 主进程按输入顺序写回结果
- 保证输出文件行顺序与输入一致
- 保持原有 summary 统计逻辑
- `--workers 1` 时兼容原串行模式

运行示例：

```bash
python3 quality_checks_V3_5/clean_variation_chars.py \
  --input input.jsonl \
  --output input.v3_5.cleaned.jsonl \
  --fields content \
  --add-cleaning-meta \
  --workers 8 \
  --chunksize 16 \
  --summary clean_summary.json
```

本地验证结果：

- `--workers 1` 与 `--workers 4` 输出文件逐字节一致
- summary 中核心统计一致
- issue 行、样例、变更统计一致
- 本地 8 核机器测试中，多进程版本相比串行有明显加速

## 5. 运行建议

本机为 8 核，推荐本地运行：

```bash
--workers 6
```

如果服务器 CPU 和内存充足，可以使用：

```bash
--workers 8
```

或根据机器规格提升到：

```bash
--workers 12
--workers 16
```

如果单条样本特别长，或者机器内存压力较大，建议先从：

```bash
--workers 4
```

开始测试，再逐步提高并发数。

## 6. 当前产出

本周已完成：

- V3.5 清洗规则能力梳理
- 质量检查脚本运行方式确认
- JSONL 清洗脚本多进程改造
- 串行 / 多进程结果一致性验证
- 服务器运行命令整理
- 可清洗 case 与可检测 case 文档化
